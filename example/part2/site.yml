- hosts: server
  vars:
    db_password: test
  roles:
    - packages
    - db

- hosts: server
  vars:
    app_name: facetweet
    home_dir: /home/admini/
    db_password: test
    debug: False
    database_uri: "dburi"
    base_url: "blah"
    secret_key: "blahblah"
  roles:
    - code
    - build